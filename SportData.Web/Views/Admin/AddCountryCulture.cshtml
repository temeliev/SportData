@model SportData.Web.Models.Admin.CountryCultureViewModel
@{
    ViewBag.Title = "AddCountryCulture";
}

<div class="row" Id="test">
    @Html.Partial("_AdminList")
    <div class="col-lg-8 col-md-8 col-sm-7">
        @using (Html.BeginForm("AddCountryCulture", "Admin", FormMethod.Post, new { @class = "form-horizontal", enctype = "multipart/form-data" }))
        {
            @*<form class="form-horizontal">*@
            <fieldset>
                @Html.ValidationSummary(true)
                @Html.HiddenFor(x => x.CountryId)
                <div class="form-group">
                    @Html.LabelFor(m => m.CountryName, new { @class = "col-lg-2 control-label" })
                    @*<label for="inputName" class="col-lg-2 control-label">Име</label>*@
                    <div class="col-lg-10">
                        @Html.EditorFor(x => x.CountryName, new { htmlAttributes = new { @class = "form-control", @placeholder = "Наименование" } })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.CultureName, new { @class = "col-lg-2 control-label" })
                    <div class="col-lg-10">
                        @Html.DropDownListFor(m => m.CultureId, new SelectList(ViewBag.Cultures, "Value", "Text"), "  ----- Избери -----  ", new { @class = "form-control", @id = "culture" })
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-lg-10 col-lg-offset-2">
                        <a href=@Model.PreviousLink class="btn btn-default">Назад</a>
                        <button type="submit" class="btn btn-primary">Запази</button>
                    </div>
                </div>
            </fieldset>
        }
    </div>
</div>


