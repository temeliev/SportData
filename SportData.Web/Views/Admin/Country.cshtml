@model SportData.Web.Models.Admin.CountryViewModel
@{
    ViewBag.Title = "Country";
}

<div class="row" Id="test">
    @Html.Partial("_AdminList")
    <div class="col-lg-8 col-md-8 col-sm-7">
        @using (Html.BeginForm("AddCountry", "Admin", FormMethod.Post, new { @class = "form-horizontal", enctype = "multipart/form-data" }))
        {
            @*<form class="form-horizontal">*@
            <fieldset>
                @Html.ValidationSummary(true)
                <div class="form-group">
                    @Html.LabelFor(m => m.CountryName, new { @class = "col-lg-2 control-label" })
                    @*<label for="inputName" class="col-lg-2 control-label">Име</label>*@
                    <div class="col-lg-10">
                        @Html.EditorFor(x => x.CountryName, new { htmlAttributes = new { @class = "form-control", @placeholder = "Наименование" } })
                        @*<input type="text" class="form-control" id="inputName" placeholder="Наименование">*@
                    </div>
                </div>
                <div class="form-group">
                    <label for="inputImageUrl" class="col-lg-2 control-label">Иконка</label>
                    <div class="col-lg-10">
                        <input type="url" class="form-control" id="inputImageUrl" placeholder="Линк">
                    </div>
                </div>
                <div class="form-group">
                    <label for="location" class="col-lg-2 control-label">Локация</label>
                    <div class="col-lg-10">
                        @Html.DropDownListFor(m => m.ParentId, new SelectList(Model.Locations, "Value", "Text"), "  ----- Избери -----  ", new { @class = "form-control", @id = "location" })
                    </div>
                </div>
                <div class="form-group">
                    <label for="culture" class="col-lg-2 control-label">Език</label>
                    <div class="col-lg-10">
                        @Html.DropDownListFor(m => m.CultureId, new SelectList(Model.Cultures, "Value", "Text"), "  ----- Избери -----  ", new { @class = "form-control", @id = "culture" })
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-lg-10 col-lg-offset-2">
                        <button type="reset" class="btn btn-default">Изчисти</button>
                        <button type="submit" class="btn btn-primary">Запази</button>
                    </div>
                </div>
            </fieldset>
            @*</form>*@
        }
    </div>
</div>
